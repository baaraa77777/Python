num_char = len(input('What is your name?'))

#changing to string as we can't concatenate strings and integers.
print("Your name has " + str(num_char) +"characters.")

#checking type 
print(type(num_char))

#converting to floating type.
print(float(num_char))